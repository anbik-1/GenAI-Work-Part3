{
  "name": "@anycompanyread/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anycompanyread/shared": "*",
    "@aws-sdk/client-cognito-identity-provider": "^3.550.0",
    "@aws-sdk/client-dynamodb": "^3.550.0",
    "@aws-sdk/lib-dynamodb": "^3.550.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.136",
    "typescript": "^5.4.5"
  }
}
